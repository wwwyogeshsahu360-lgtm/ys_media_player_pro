/// core/routing
/// ============
/// Contains route path definitions so that paths are not hard-coded
/// all over the codebase.
class AppRoutes {
  const AppRoutes._();

  /// Initial splash route.
  static const String splash = '/';

  /// Main home shell route.
  static const String home = '/home';
}
